import 'package:flutter/material.dart';

class PlaylistScrenn extends StatelessWidget {
  const PlaylistScrenn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
